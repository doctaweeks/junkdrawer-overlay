From 503cac479ea63b7084be96a26c9ccabdc0d88206 Mon Sep 17 00:00:00 2001
From: "Daniel M. Weeks" <dan@danweeks.net>
Date: Thu, 16 Nov 2017 17:00:23 -0500
Subject: [PATCH] Do not automagically depend on dbus

Signed-off-by: Daniel M. Weeks <dan@danweeks.net>
---
 configure.ac | 7 +++++--
 1 file changed, 5 insertions(+), 2 deletions(-)

diff --git a/configure.ac b/configure.ac
index 0d1bf8103..96a91dc79 100644
--- a/configure.ac
+++ b/configure.ac
@@ -736,8 +736,11 @@ AS_IF([test "$enable_plugins" = yes],[
 ])
 AM_CONDITIONAL(ENABLE_PLUGINS,[test "$enable_plugins" = yes])
 
-with_dbus=no
-AS_IF([test "$enable_plugins" != no],[
+AC_ARG_WITH([dbus], [AS_HELP_STRING([--with-dbus], [build with dbus support])],
+            [],
+            [with_dbus=yes])
+
+AS_IF([test "$with_dbus" != no],[
   PKG_CHECK_MODULES([DBUS],
     [dbus-1 >= 1.3],
     [AC_DEFINE(DBUS, 1, [Build with dbus support?]) with_dbus=yes],
-- 
Daniel M. Weeks

